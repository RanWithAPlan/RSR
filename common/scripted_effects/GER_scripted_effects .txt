GER_calculate_unity_effects = {			
		set_variable = {
			var = GER_unity_pp_gain
			value = unity_number
		}
		subtract_from_variable = {
			var = GER_unity_pp_gain
			value = 50
		}
		divide_variable = {
			var = GER_unity_pp_gain
			value = 100
		}
		set_variable = {
			var = GER_unity_stability_factor
			value = unity_number
		}
		subtract_from_variable = {
			var = GER_unity_stability_factor
			value = 50
		}
		divide_variable = {
			var = GER_unity_stability_factor
			value = 200
		}
}